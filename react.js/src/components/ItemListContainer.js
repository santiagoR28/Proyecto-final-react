import "../components/nv.css";

const ILC = ({ greeting }) => {
  return (
    <h1>
      Bienvenido a <spam class="name">{greeting}</spam>, la mejor ropa al mejor
      precio.
    </h1>
  );
};

export default ILC;
